#ifndef NUMBERS_H
#define NUMBERS_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void generate_numbers(int min, int max, int uniqc);
int ask_arguments(int *min, int *max, int *uniqc);

#endif
